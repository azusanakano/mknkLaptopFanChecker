namespace Mknk.LaptopFanChecker
{
    internal static class AppInfo
    {
        // Keep the GUI, tray, reports and managed EXE metadata on the same version.
        public const string Version = "1.3.1";
        public const string AssemblyVersion = Version + ".0";
        public const string VersionLabel = "v" + Version;
        public const string ProductName = "ネコシステム社 CPUファンチェッカー";
    }
}
