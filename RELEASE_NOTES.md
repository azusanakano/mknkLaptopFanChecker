# Release notes

## v1.2.1 — バージョン表示

- タイトルバーと画面右上へバージョン番号を常時表示
- ウィンドウ幅に合わせてメイン列を伸縮させ、最小サイズでも右上の番号が隠れないよう修正
- タスクトレイアイコンにマウスを重ねるとバージョン番号を表示
- 画面、トレイ、HTMLレポート、検査GUIのEXE情報は共通のバージョン定義を使用
- ネイティブ起動EXEのバージョン情報と配布パッケージも1.2.1へ更新

## v1.2.0 — ネコシステム社ブランド・GitHub公開版

- 画面、タスクトレイ、EXE情報、センサー一覧、HTMLレポートを「ネコシステム社」ブランドへ統一
- GitHub ActionsによるWindowsビルド、テスト、DLL依存検査、配布ZIP生成を追加
- `.gitignore`、`.gitattributes`、公開手順、セキュリティ方針、コントリビューション案内を追加
- 内部IDと設定保存先は旧版との互換性のため維持
- 1450×1100画面、CPU基本速度、CPU温度修復、自動起動、タスクトレイ常駐、ゆりちゃんアイコンを維持
- Win32ランチャーは静的リンクを維持し、`libc++.dll` と `libunwind.dll` は不要

## v1.1.9 — ネイティブ起動ランタイム修正版

- 最上位の64bit Win32起動EXEをC++ランタイムの静的リンクで再ビルド
- `libc++.dll` と `libunwind.dll` が無いPCで起動できない問題を修正
- EXEのインポートテーブルを検査し、両DLLへの依存がないことを確認
- v1.1.8のCPU温度取得、PawnIO 2.2.0、自動起動、ゆりちゃんアイコン、画面修正を維持

## v1.1.8 — CPU温度取得・自動起動修正版

- CPU温度が未検出になる原因となった、通常権限のGUIから管理者専用PawnIOデバイスへ接続する起動方式を修正
- 公式PawnIOを2.1.0から2.2.0へ更新し、修復インストールとセンサー再検出を画面から実行可能にした
- PawnIO導入、バージョン、接続、管理者権限、CPU温度センサー列挙の状態から未検出理由を表示
- 温度を取得できないときは「監視中」ではなく「監視不可」と表示し、負荷テストを禁止
- 自動起動を最高権限のログオンタスクに戻し、ユーザー別 `Run` の非昇格監視起動でタスクを二重に起動確認
- 1450×1100の初期サイズ、自動CPUファンテスト欄のレイアウト、CPU基本速度、ゆりちゃんアイコンを維持

## v1.1.7 — ゆりちゃん専用アイコン版

- 利用者提供の写真と三面図を基に、白い顔、額のグレーと薄茶、中央の白い筋、黄緑色の大きな目、ピンクの鼻を反映した「ゆりちゃん」専用アイコンへ変更
- CPUファンを示す水色の3枚羽根を組み合わせ、16、20、24、32、40、48、64、128、256pxの各サイズを収録
- 最上位のWin32ネイティブEXE、検査GUI、ウィンドウ、タスクバー、通知領域へ同じアイコンを登録
- v1.1.6のユーザー別 `Run` 自動起動、ログオン時の監視専用モード、タスクスケジューラ旧登録からの移行処理を維持

## v1.1.6 — ログオン自動起動・専用アイコン修正版

- 自動起動を、実行されない場合があった最高権限のタスクスケジューラ方式から、Windows公式のユーザー別 `Run` 方式へ変更
- 最上位のWin32ネイティブEXEへ `--monitor` を付けて登録し、空白・日本語を含む保存場所でも正しいコマンドを保存後に照合
- v1.1.5以前の登録済みタスクと、更新前の古い保存先を通常起動時に検出して新方式へ自動移行
- 検査GUIを通常権限で起動し、PawnIO導入プログラムだけを明示的に管理者昇格
- ログオン自動起動時はドライバー確認画面やUACを表示せず、負荷テストも実行しない仕様を明確化
- 三毛猫とCPUファンを組み合わせた専用アイコンを作成し、ネイティブEXE、検査GUI、ウィンドウ、タスクバー、通知領域へ登録
- 16、20、24、32、40、48、64、128、256pxを含むWindows用マルチサイズICOを同梱

## v1.1.5 — 自動起動修正版

- 更新・移動後も古い実行ファイルを指していた自動起動タスクを、通常起動時に現在の保存場所へ自動修復
- タスク登録を単一の `/TR` コマンド文字列からWindowsタスクスケジューラXMLへ変更
- 実行ファイル、`--monitor` 引数、作業フォルダーを別々に登録し、空白・日本語を含むパスの扱いを安定化
- バッテリー駆動中も起動・監視を継続する設定を明示
- 常時監視がタスクスケジューラの既定実行時間で終了しないよう、実行時間制限を解除
- ログオンユーザー限定、最高権限、重複起動抑止、監視のみという既存の安全仕様を維持

## v1.1.4 — Windowsパフォーマンス基本速度版

- CPU基本速度の取得元をWMI `Win32_Processor.MaxClockSpeed` から変更
- Windows `CallNtPowerInformation(ProcessorInformation)` の `MaxMhz` を使用
- API取得失敗時は異なる値へ置換せず「取得不可」と表示
- v1.1.3の［×］／［最小化］によるタスクトレイ常駐を維持
- 1450×1100中央表示と、自動CPUファンテスト欄の重なり修正を維持

## v1.1.3 — CPU基本速度・常時タスクトレイ版

- CPU情報欄へWindowsが報告する基本速度を追加
- HTML検査レポートとセンサー一覧にもCPU基本速度を記録
- 通常起動時も［×］で終了せず、タスクトレイへ収納して監視を継続
- ［最小化］でもタスクバーから隠し、タスクトレイへ収納
- 完全終了はタスクトレイの右クリックメニュー「終了」に統一
- v1.1.2の1450×1100中央表示と、v1.1.1の表示重なり修正を維持

## v1.1.2 — 初期ウィンドウサイズ調整版

- 通常起動時の最大化を解除
- 提示された画面例に合わせ、初期サイズを1450×1100へ変更
- 初期表示位置を画面中央へ変更
- v1.1.1で修正した自動CPUファンテスト欄の文字重なり対策を維持
- CPUファン検査、発熱監視、保護停止、レポート処理には変更なし

## v1.1.1 — 画面レイアウト修正版

- 通常起動時の初期表示を最大化し、右側の設定・記録欄が途中で切れにくいよう修正
- 最大化解除時の基準サイズを1180×820から1600×1020へ変更
- 「検査時間」「CPU負荷」のラベルが選択欄へ52px重なっていた問題を修正
- 固定位置ラベルを94px幅に調整し、選択中の文字列が完全に表示されるよう修正
- CPUファン検査、異常発熱監視、センサー取得、レポートの処理内容には変更なし

## v1.1.0 — Windowsネイティブ起動版

- 最上位へC++製64bit Win32 GUIランチャーを追加
- `mknkLaptopFanChecker.exe` のダブルクリックだけで起動可能
- CMD／PowerShellを通常起動経路から廃止
- `--demo`、`--monitor`などのコマンドライン引数を検査GUIへ引き継ぐ処理を追加
- 実行場所に空白や日本語が含まれていても動作するUnicodeパス対応
- ランチャー用のDPI対応マニフェスト、アイコン、バージョン情報を追加
- 既存のCPUファン検査、92℃保護停止、常駐監視、通知、レポート機能を維持

## v1.0.0

- 初回リリース
- LibreHardwareMonitor 0.9.6 / PawnIO対応
- RPM直接判定と温度挙動による間接判定
- ログオン時の自動起動・異常発熱監視
